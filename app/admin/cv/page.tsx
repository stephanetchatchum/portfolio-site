export default function AdminCvPage() {
  // TODO: "Generate CV" button -> calls /api/cv/generate,
  // shows editable draft, "mark as public download" toggle
  return <main className="mx-auto max-w-4xl px-6 py-12">Generate CV</main>;
}
